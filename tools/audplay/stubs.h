/* this file contains dummy implementations of classes that aren't relevant to
 * the program, but are needed due to external linkage dependencies.
 */

class MessagePool
{
public:
    void postMessage(const char* msg);
};

